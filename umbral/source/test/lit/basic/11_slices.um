# RUN: %uc %s --dump-ir | %FileCheck %s

@extern const rt_fmt_print_line : fn([]u8, []i32) -> void;

const make_arr := fn() -> [3]i32 {
  return [3]i32{ 1, 2, 3 };
}

const main := fn() -> void {
  const a := make_arr();
  var x := a[1];
}

# CHECK: define [3 x i32] @
# CHECK: define i32 @main
