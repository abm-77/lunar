# RUN: %uc %s --root %S/../../../std --dump-ir 2>/dev/null | %FileCheck %s

# cross-module named struct init: mod::Type { field = value }

import math.types;

const main := fn() -> void {
  const v := types::vec2 { x = 3.0, y = 4.0 };
}

# CHECK: define i32 @main
