# RUN: %uc %s --root %S/../../../std --dump-ir 2>/dev/null | %FileCheck %s

# cross-module vec construction: mod::Type(args)

import math.types;

const main := fn() -> void {
  const v := types::vec2(3.0, 4.0);
}

# CHECK: define i32 @main
