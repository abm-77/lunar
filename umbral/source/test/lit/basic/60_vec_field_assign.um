# RUN: %uc %s --root %S/../../../std -o %t && %t | %FileCheck %s
# CHECK: 10 20

import sys.io.fmt;

const main := fn() -> i32 {
  var v: vec<f32, 2>;
  v.x = 10.0;
  v.y = 20.0;
  fmt::println("{} {}", [2]fmt::Arg{
    fmt::arg_f32(v.x), fmt::arg_f32(v.y)
  });
  return 0;
}
