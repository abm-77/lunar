import math.types;

# Shader — opaque wrapper around a u64 asset ID for a compiled .umsh bundle.
@pub const Shader<T: type> := struct { id: u64 }

impl Shader<T> {
  const create() -> Shader<T> {
    return Shader<T>{ id = @shader_ref(T) };
  }
}
