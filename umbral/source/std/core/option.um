@pub const Option<T: type> := struct {
  has_value: bool,
  value: T,
}

impl Option<T> {
  const create := fn(v: T) -> Option<T> {
    return Option<T> {
        has_value = true,
        value = v,
    };
  }

  const none := fn() -> Option<T> {
    return Option<T> {
        has_value = false,
    };
  }
}
